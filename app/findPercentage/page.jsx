import { FindPercentage } from '@/src/components/FindPercentage';

export const metadata = {
  title: 'Find Percentage - MAKAUT GPA Calculator',
  description: 'Convert SGPA, YGPA, or credit points to percentage.',
};

export default function FindPercentagePage() {
  return <FindPercentage />;
}
